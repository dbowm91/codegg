[package]
name = "egglsp-test-server"
version = "0.1.0"
edition = "2021"
description = "Deterministic fake LSP server for egglsp integration testing."
license = "MIT"
rust-version = "1.81"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"