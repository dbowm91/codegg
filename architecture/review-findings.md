# Architecture Documentation Review — Systematic Audit of All 81 Docs

Generated by reviewing every architecture doc in `architecture/` against each
other and against the codebase. Organized by severity.

## Critical: Cross-Doc Inconsistencies

These are factual contradictions between docs that will mislead readers.

| Issue | Docs Affected | Detail |
|-------|--------------|--------|
| `STORAGE_LAYOUT_VERSION` drift | `workspace_services.md` (says 25), `jobs.md` (says 35), `storage.md` (says 35) | **Fixed**: `workspace_services.md` updated to 35. |
| Event count mismatch | `bus.md` header says 45, table sums to 41 | 41 variants across the table rows (7+2+3+3+2+2+3+4+2+4+9). Header is wrong or table is incomplete. |
| Theme count mismatch | `theme.md` says "~45 Halloy-format themes", `tui.md` says "31 themes in `theme.rs`" | Likely different things (bundled vs compiled) but ambiguous. |
| Tool count inconsistency | `tool.md` overview says "40 tools", later says "~30 tools" | Also inconsistent with AGENTS.md which says ~38. Pick one number and use it everywhere. |
| `CANONICAL_PATHS_CACHE` TTL | `permission.md` says 1s, `security.md` says 300s | **Fixed**: `permission.md` updated to 300s (matches source). |

## Critical: Doc Staleness

These docs have not been updated to reflect current implementation.

| Doc | Issue |
|-----|-------|
| `tool_program_language.md` | Frozen at M004. Missing `submit_job()` (M007), TP998/TP999 error codes. Says "Version: 1" but language has evolved. |
| `session.md` | Migration docs end at v22; `storage.md` documents through v35. |
| `codegg_core.md` | Lists 12 modules but is missing `workspace`, `workspace_services`, `migration`, `run_store`, `jobs`. Only 52 lines. | **Fixed**: Updated to list 28 modules. |
| `provider.md` | Key Files table has duplicated path prefix (`crates/codegg-providers/crates/codegg-providers/src/`). | **Fixed**: Path prefix corrected. |
| `model-adapters.md` | 39 lines — minimal detail on what transforms are supported. |

## High: Self-Referential "See Also" Links

These docs point to themselves in their "See Also" section, providing no value.

- `exec.md` → references itself
- `command.md` → references itself
- `memory.md` → references itself
- `snapshot.md` → references itself
- `tts.md` → references itself
- `bus.md` → references itself

## High: Missing Cross-References

These docs lack "See Also" sections or have empty ones.

| Doc | Missing References |
|-----|-------------------|
| `model_profile_task_state.md` | No cross-references at all |
| `identity.md` | No cross-references section |
| `auth.md` | No cross-references section |
| `goal.md` | No cross-references section |
| `theme.md` | No cross-references section |
| `workspace_services.md` | "See Also" section is empty (line 320) |
| `agent-tool-surface.md` | No cross-references section |
| `codegg_core.md` | No cross-references section |

## High: Execution Order Gaps

These docs describe subsystems that interact but don't document the execution ordering.

| Gap | Docs Affected |
|-----|--------------|
| Preflight vs ToolBroker execution order | `preflight.md` says tools call preflight directly; `tool_broker.md` says broker is the canonical boundary. Which runs first? |
| Snapshot auto-rollback not implemented | `snapshot.md` explicitly calls this out as a gap; `run_store.md` mentions `can_rollback` is disabled. |
| PermissionRegistry vs PermissionStore relationship | `permission.md` documents PermissionStore; `bus.md` documents PermissionRegistry. The relationship between them is unclear. |

## Medium: Structural Issues

| Doc | Issue |
|-----|-------|
| `agent.md` | Two `## 10` heading sections (Prompt Assembly and Background scheduling). Step 14 missing from Main Execution Flow (jumps 13→15). |
| `tool_programs.md` | 1624 lines mixing normative spec, implementation details, and milestone changelogs. Should be split. |
| `tool_broker.md` | 145 lines for the most critical execution boundary. Missing `Tool::contract()` definition, `BrokerAuthority` type, and `ToolContract`/`ToolCallerPolicy`/`ToolEffectClass` definitions. |
| `lsp.md` | ~1900 lines covering 17 phases. Could use a phase status summary table. "Supported Languages" table says 41 servers but only lists ~20. |
| `plugin.md` | 1631 lines. Could be split into subsystem docs. |
| `human_shell.md` | Phase numbers skip Phase 6 (5+6 combined). Operational validation section with commit hashes is runbook content, not architecture. |

## Medium: Naming / Conceptual Confusion

| Issue | Detail |
|-------|--------|
| Two `Command` structs | `src/command/mod.rs::Command` and `src/tui/command.rs::Command` are different types. Documented but could confuse. |
| `shell_session` naming | `shell_session.md` is about metadata, not shell execution. Name collides with human shell's output stores. |
| mDNS service type | Uses `_opencode._tcp.local.` — naming inconsistency with "codegg" branding. |
| Process-backed commands | Reuse `TuiCommand::PluginCommandRun` variant — semantically misleading. |
| `RouteToNativeTool` residual | `command_routing.md` says all git maps to `RouteToGit` but `RouteToNativeTool` still exists. What uses it? |

## Low: Minor Issues

| Doc | Issue |
|-----|-------|
| `exec.md` | No mention of how exec handles file modifications, workspace detection, or multiple prompts. |
| `tts.md` | macOS-only, `init()` only handles `TtsProvider::None` (no-op). Provider enum is pointless. |
| `upgrade.md` | `upgrade()` function exists but is never called. `autoupgrade` config defaults to `true` but is unwired. |
| `shell_session.md` | 81 lines. In-memory only, no eviction, no lifecycle discussion. |
| `worktree.md` | `is_locked` and `is_main` fields documented as "not implemented". |
| `run_store.md` | `can_rollback` disabled, `RerunDescriptor` defined but re-execution not implemented. |
| `resilience.md` | No mention of circuit breaker state persistence across daemon restarts. |
| `crypto.md` | `EncryptedData` struct fields are `pub`. No documentation of nonce generation strategy. |
| `research.md` | No error recovery for partial pipeline failures. No rate limiting for LLM-backed operations. |

## Recommended Priority Actions

1. **Reconcile `STORAGE_LAYOUT_VERSION`** — Update `workspace_services.md` from 25 to 35 (or confirm 25 is correct).
2. **Fix event count** in `bus.md` header (41, not 45).
3. **Update `tool_program_language.md`** — Add `submit_job()`, TP998/TP999, bump version.
4. **Update `session.md`** — Document migrations v23–v35.
5. **Fix self-referential "See Also"** links in 6 docs.
6. **Add missing cross-references** to 8 docs.
7. **Document preflight/broker execution order** in both docs.
8. **Fix `agent.md` heading numbering** (two `## 10` sections, missing step 14).
9. **Normalize tool count** to a single number across all docs.
10. **Fix `provider.md` key files path duplication**.
