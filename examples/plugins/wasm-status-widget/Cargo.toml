[package]
name = "wasm-status-widget"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
codegg-plugin-sdk = { path = "../sdk-rust" }
codegg-protocol = { path = "../../../crates/codegg-protocol" }
serde_json = "1"

[workspace]
