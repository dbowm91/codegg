[fixture]
name = "pytest_failure"
command = "pytest"
exit_code = 1
stream = "stderr"

[expect]
must_contain = ["FAILED", "AssertionError", "ZeroDivisionError"]
must_not_contain = []
source_spans = ["tests/test_math.py"]
failed_tests = ["tests/test_math.py::test_add", "tests/test_math.py::test_divide"]
max_projected_tokens_safe = 2000
max_projected_tokens_aggressive = 1000
