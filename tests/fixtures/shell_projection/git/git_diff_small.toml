[fixture]
name = "git_diff_small"
command = "git diff"
exit_code = 0
stream = "stdout"

[expect]
must_contain = ["diff --git", "subtract", "@@"]
must_not_contain = []
source_spans = ["src/lib.rs"]
failed_tests = []
max_projected_tokens_safe = 500
max_projected_tokens_aggressive = 500
