# Exec Module Override

This file contains exec-specific guidance and overrides root AGENTS.md.

## Exec Mode

Non-interactive exec mode for CI/CD with JSON I/O. Use `opencode exec --json` for machine-readable output.